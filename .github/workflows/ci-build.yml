name: CI - Tests + Build Docker

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Start Flask server in background
        run: |
          python app.py &
          sleep 5   # donne le temps au serveur de démarrer

      - name: Run pytest
        run: pytest tests/ -v

      - name: Stop Flask server
        if: always()   # s'exécute même si les tests échouent
        run: pkill -f "python app.py" || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/flask-api-render:latest .
          docker push ghcr.io/${{ github.repository_owner }}/flask-api-render:latest
